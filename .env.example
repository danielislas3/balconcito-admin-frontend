# Frontend API base (apunta a Nuxt server routes, que proxean a Rails lo no migrado)
NUXT_PUBLIC_API_BASE=http://localhost:3000/api/v1

# PostgreSQL (misma BD que usa Rails)
DATABASE_URL=postgresql://user:password@localhost:5432/database_name

# Backend Rails (proxy temporal durante migración)
NUXT_RAILS_API_URL=http://localhost:3001/api/v1
